# code

Reusable source for the research track — library code meant to be imported, not run
once. Anything specific to a prize submission belongs in `../eip/code/` or
`../fisher/code/` instead, so that revisiting a submission never requires touching
research code and vice versa.

Empty at present. Expected first occupants: spatial weights construction, spectral
utilities for the band-matrix work in [`../theory`](../theory).
